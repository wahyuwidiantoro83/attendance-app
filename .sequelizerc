const path = require("path");

module.exports = {
  config: path.resolve("apps/api/src/config", "config.js"),
  "models-path": path.resolve("apps/api/src", "models"),
  "migrations-path": path.resolve("apps/api/src", "migrations"),
  "seeders-path": path.resolve("apps/api/src", "seeders"),
};
